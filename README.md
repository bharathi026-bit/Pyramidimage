# Pyramidimage
This program uses the OpenCV library to create an image pyramid by repeatedly reducing the size of the original image using pyrDown(). It then displays different pyramid levels and detects edges at each level using the Canny edge detection method. Finally, it shows the image sizes and the detected edges for comparison at every pyramid level.
